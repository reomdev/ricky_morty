Los Hooks mas utilizados:

useState
useEffect
useContext
useReducer
useMemo
useRef
useCallback
Custom Hooks


Instalamos 
    react , react-dom

Instalamos 
    webpack webpack-cli webpack-dev-server

Instalamos los loader y los plugins (el mas basico es el de HTML)

    html-webpack-plugin 
    html-loader

Babel
    babel-loader
    @babel/preset-env
    @babel/core
    @babel/preset-react

Luego...

Configuramos:

- Punto de entrada (entry)
- Punto de salida (output)
- Hay que tener un resolve de las extensiones
que vamos a utilizar (resolve).

*Configuramos la seccion module:

- Ahora viene el modulo que va a tener
las reglas (rules de HTML, CSS, SASS, JS)

*Luego configuramos los plugins

*Luego la seccion de devServer

Despues creamos el archivo .babelrc y configuramos
los presets

*Configuras el Dotenv de webpack

*Despues configuras el loader de css que vayas a utilizar
por lo general es css-loader y el mini-css-extract-plugin 
que es al que le configuramos el filename ('assets/[name].css')

Para un mejor desarrollo se configura eslint y prettier.

npm i 
eslint 
@babel/eslint-parser 
eslint-config-airbnb 
eslint-plugin-import 
eslint-plugin-jsx-a11y 
eslint-plugin-react

*Creamos un archivo .eslint en la raiz del proyecto y le 
pasamos la configuracion

Instalamos prettier

npm i 
prettier 
eslint-plugin-prettier 
eslint-config-prettier

*Creamos un archivo .prettierrc y le pasamos la configuracion


*Si trabajamos con imagenes es importante usar copy-webpack-plugin para poder
copiar los files en la carpeta donde el proyecto hara build






